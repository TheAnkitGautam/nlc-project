import { Box, Typography } from "@mui/material"

const AddNewContent = () => {
    return (
        <Box sx={{ height: "80vh" }}>
            <Typography variant="h4" color="initial">Add New Post</Typography>
        </Box>
    )
}

export default AddNewContent